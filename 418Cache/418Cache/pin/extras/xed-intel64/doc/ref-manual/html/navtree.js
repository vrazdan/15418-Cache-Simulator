var NAVTREE =
[
  [ "XED", "index.html", [
    [ "XED User Guide", "index.html", [
      [ "Introduction", "index.html#INTRO", null ],
      [ "Table of Contents", "index.html#TOC", null ],
      [ "Building your program using XED.", "index.html#BUILD", null ],
      [ "External Requirements", "index.html#EXTERN", null ],
      [ "Terminology", "index.html#TERMS", null ],
      [ "Overview of XED approach", "index.html#OVERVIEW", null ],
      [ "Instruction classes", "index.html#ICLASS", [
        [ "Special Cases", "index.html#SPECIAL", null ],
        [ "NOPs", "index.html#NOPs", null ]
      ] ],
      [ "Operands", "index.html#X86-OPERANDS", [
        [ "AVX512 Operands", "index.html#AVX512_OPERANDS", null ],
        [ "Operand Resource Visibilities", "index.html#OPERAND_VISIBILITY", null ],
        [ "x87 Register stack popping", "index.html#X87_REG_STACK", null ],
        [ "Pseudo Resources", "index.html#PSEUDO_RESOURCES", null ],
        [ "Immediates and Displacements", "index.html#IMM_DIS", null ]
      ] ],
      [ "API Reference", "index.html#API_REF", null ],
      [ "Questions? Bugs?", "index.html#FEEDBACK", null ],
      [ "Disclaimer and Legal Information", "index.html#LEGAL", null ]
    ] ],
    [ "Modules", "modules.html", "modules" ],
    [ "Data Structures", null, [
      [ "Data Structures", "annotated.html", "annotated" ],
      [ "Data Structure Index", "classes.html", null ],
      [ "Data Fields", "functions.html", [
        [ "All", "functions.html", "functions_dup" ],
        [ "Variables", "functions_vars.html", "functions_vars" ]
      ] ]
    ] ],
    [ "Files", null, [
      [ "File List", "files.html", "files" ],
      [ "Globals", "globals.html", [
        [ "All", "globals.html", "globals_dup" ],
        [ "Functions", "globals_func.html", "globals_func" ],
        [ "Variables", "globals_vars.html", null ],
        [ "Typedefs", "globals_type.html", null ],
        [ "Enumerations", "globals_enum.html", null ],
        [ "Enumerator", "globals_eval.html", "globals_eval" ],
        [ "Macros", "globals_defs.html", null ]
      ] ]
    ] ]
  ] ]
];

var NAVTREEINDEX =
[
"annotated.html",
"group__DEC.html#gabbc8dec2bddec619bfe8f9a39c7ffdf4",
"group__ENUM.html#ga2e1b95b3b4b059703a3be3780a990ade",
"group__OPERANDS.html#ga0f1312a0bf168f19e474fbbbb493674d",
"structxed__encoder__iforms__s.html#a7aea255eacbdcfe740a5d0c8c52412c6",
"unionxed__encoder__prefixes__t.html#a17e46718b7ea9fa441204e8f5c9c2cd0",
"xed-category-enum_8h.html#a3e6e23a35a7ad463e2422d7db98e49f5a99c9bccdb53a91f7b634643c994a5948",
"xed-extension-enum_8h.html#ae7b9f64cdf123c5fda22bd10d5db9916ada8c5e2856ff780c9c3453e2ee73e815",
"xed-iclass-enum_8h.html#ad318511ae9cc50f102251b3c91a1ab9fa1de0ebfbc69643779e1c6985cc7af881",
"xed-iclass-enum_8h.html#ad318511ae9cc50f102251b3c91a1ab9fa4c146195dad85a63ae853b379c7eb968",
"xed-iclass-enum_8h.html#ad318511ae9cc50f102251b3c91a1ab9fa7768b421a697cb39d70a00a14d292cbe",
"xed-iclass-enum_8h.html#ad318511ae9cc50f102251b3c91a1ab9faa050c7adec90385356eb48c252ef2dc9",
"xed-iclass-enum_8h.html#ad318511ae9cc50f102251b3c91a1ab9facd3c4d2b445446a747a8a59bec0fd1a4",
"xed-iclass-enum_8h.html#ad318511ae9cc50f102251b3c91a1ab9fafb4610cb7f54a303423e111c00505008",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a08bab46e413d19931e5514ed9c7c2356",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a133ecbe0d28b69abe309eb45d9d518d7",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a1db118e5b085f7b5bbbf3240510cbe20",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a2820e2857f009550cb7aef9f7b62d516",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a31cc537bf2cddb2b5f1b247a1fdf6809",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a3c8552d32e0a1fac934f420d1eebe9b4",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a474c9ac52388e12d3e128497aacdf937",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a525e50ab7838a4d503d95a32cc68194b",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a5d93cf834c75e9281782ee2fd8453949",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a68ca0bdc8c6c7ab6b0f7651689d068d1",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a736e48b96bdd4fa104c7668f66685f2f",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a7d797b49c43566d965f2746c9e044077",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a8844872e8b75a43803e989f373c3945b",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a942619eac260fa45396e7723e33c4ece",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21a9fe5551d420e8aacaeda332ae6464fb4",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21aa9d89e67ba9ee30e01f4fc08042df765",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21ab55d4e5e73eefbd4598a7508dfec04a8",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21ac117d84f78befbed29658afd846f347e",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21acbfcc213f0ac1b4a5eaaaaa2f1d9af9f",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21ad6b09f17342de904adcbcec797697508",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21ae2b1cce64168b366fe904238bb4a0fc4",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21aed474a5d90c18a8eac7148c8bd2689dd",
"xed-iform-enum_8h.html#af48986d6e090a75f7ab50e6d4a182e21af77eb283fee322bb110b3f5d43d82ef7",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36a06ee7cbe1f89cb8e284f34321dfa9d44",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36a1c2c85b63d4286afdc3745569b0bb533",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36a31f59a1486e9751c64d36ca4b4cc2e31",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36a479a37db30e354265d27b130b347209c",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36a5e290bb9214171bb5da1fa694365127d",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36a745c13d84b2c8c2ac4112186a03fa7e2",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36a89e6eb80e43de553a605aa8e160d73e8",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36a9ed0d5626b6ae7548ab146087809cb6f",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36ab493668d2e7957b59ea1fc6f1ee49f2e",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36acd094f7edbc8f87f2f12b8df1b94e026",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36ae3f10e39a2bbe1bffe7b3b4599529fc9",
"xed-iformfl-enum_8h.html#a261aba9f055ef9754e8152bf28debe36afa64896a158ebba3c0ea38745df92296",
"xed-nonterminal-enum_8h.html#ae0eb8f71a8f89cddf0df4b1a6a2b27afa21fdfb3b402e91dd4ec7fe7623ace0d7",
"xed-operand-accessors_8h.html#a1e5380a740d1f1ca6d8a544b1b2b87c6",
"xed-operand-action_8h.html#a0978e9802aa6911d7c0e5c0b35cbe580",
"xed-operand-width-enum_8h.html#a4bc2ed96b8433c06c363d614a2fa1676a1b7efccd3095d73de5e689a7b4e1021e",
"xed-reg-enum_8h.html#af05c33c5a68e9304d1d8ac0408ae3f61a51160147b681fe30a1be7bea652d9a0c",
"xed-syntax-enum_8h.html#a226c37dc9f1af9e61dad79dc5f65bc9fa627bdee9fd81c454a2fd83ce0088e3a4"
];

var SYNCONMSG = 'click to disable panel synchronisation';
var SYNCOFFMSG = 'click to enable panel synchronisation';
var navTreeSubIndices = new Array();

function getData(varName)
{
  var i = varName.lastIndexOf('/');
  var n = i>=0 ? varName.substring(i+1) : varName;
  return eval(n.replace(/\-/g,'_'));
}

function stripPath(uri)
{
  return uri.substring(uri.lastIndexOf('/')+1);
}

function stripPath2(uri)
{
  var i = uri.lastIndexOf('/');
  var s = uri.substring(i+1);
  var m = uri.substring(0,i+1).match(/\/d\w\/d\w\w\/$/);
  return m ? uri.substring(i-6) : s;
}

function hashValue()
{
  return $(location).attr('hash').substring(1).replace(/[^\w\-]/g,'');
}

function hashUrl()
{
  return '#'+hashValue();
}

function pathName()
{
  return $(location).attr('pathname').replace(/[^-A-Za-z0-9+&@#/%?=~_|!:,.;\(\)]/g, '');
}

function localStorageSupported()
{
  try {
    return 'localStorage' in window && window['localStorage'] !== null && window.localStorage.getItem;
  }
  catch(e) {
    return false;
  }
}


function storeLink(link)
{
  if (!$("#nav-sync").hasClass('sync') && localStorageSupported()) {
      window.localStorage.setItem('navpath',link);
  }
}

function deleteLink()
{
  if (localStorageSupported()) {
    window.localStorage.setItem('navpath','');
  }
}

function cachedLink()
{
  if (localStorageSupported()) {
    return window.localStorage.getItem('navpath');
  } else {
    return '';
  }
}

function getScript(scriptName,func,show)
{
  var head = document.getElementsByTagName("head")[0]; 
  var script = document.createElement('script');
  script.id = scriptName;
  script.type = 'text/javascript';
  script.onload = func; 
  script.src = scriptName+'.js'; 
  if ($.browser.msie && $.browser.version<=8) { 
    // script.onload does not work with older versions of IE
    script.onreadystatechange = function() {
      if (script.readyState=='complete' || script.readyState=='loaded') { 
        func(); if (show) showRoot(); 
      }
    }
  }
  head.appendChild(script); 
}

function createIndent(o,domNode,node,level)
{
  var level=-1;
  var n = node;
  while (n.parentNode) { level++; n=n.parentNode; }
  if (node.childrenData) {
    var imgNode = document.createElement("img");
    imgNode.style.paddingLeft=(16*level).toString()+'px';
    imgNode.width  = 16;
    imgNode.height = 22;
    imgNode.border = 0;
    node.plus_img = imgNode;
    node.expandToggle = document.createElement("a");
    node.expandToggle.href = "javascript:void(0)";
    node.expandToggle.onclick = function() {
      if (node.expanded) {
        $(node.getChildrenUL()).slideUp("fast");
        node.plus_img.src = node.relpath+"ftv2pnode.png";
        node.expanded = false;
      } else {
        expandNode(o, node, false, false);
      }
    }
    node.expandToggle.appendChild(imgNode);
    domNode.appendChild(node.expandToggle);
    imgNode.src = node.relpath+"ftv2pnode.png";
  } else {
    var span = document.createElement("span");
    span.style.display = 'inline-block';
    span.style.width   = 16*(level+1)+'px';
    span.style.height  = '22px';
    span.innerHTML = '&#160;';
    domNode.appendChild(span);
  } 
}

var animationInProgress = false;

function gotoAnchor(anchor,aname,updateLocation)
{
  var pos, docContent = $('#doc-content');
  var ancParent = $(anchor.parent());
  if (ancParent.hasClass('memItemLeft') ||
      ancParent.hasClass('fieldname') ||
      ancParent.hasClass('fieldtype') ||
      ancParent.is(':header'))
  {
    pos = ancParent.position().top;
  } else if (anchor.position()) {
    pos = anchor.position().top;
  }
  if (pos) {
    var dist = Math.abs(Math.min(
               pos-docContent.offset().top,
               docContent[0].scrollHeight-
               docContent.height()-docContent.scrollTop()));
    animationInProgress=true;
    docContent.animate({
      scrollTop: pos + docContent.scrollTop() - docContent.offset().top
    },Math.max(50,Math.min(500,dist)),function(){
      if (updateLocation) window.location.href=aname;
      animationInProgress=false;
    });
  }
}

function newNode(o, po, text, link, childrenData, lastNode)
{
  var node = new Object();
  node.children = Array();
  node.childrenData = childrenData;
  node.depth = po.depth + 1;
  node.relpath = po.relpath;
  node.isLast = lastNode;

  node.li = document.createElement("li");
  po.getChildrenUL().appendChild(node.li);
  node.parentNode = po;

  node.itemDiv = document.createElement("div");
  node.itemDiv.className = "item";

  node.labelSpan = document.createElement("span");
  node.labelSpan.className = "label";

  createIndent(o,node.itemDiv,node,0);
  node.itemDiv.appendChild(node.labelSpan);
  node.li.appendChild(node.itemDiv);

  var a = document.createElement("a");
  node.labelSpan.appendChild(a);
  node.label = document.createTextNode(text);
  node.expanded = false;
  a.appendChild(node.label);
  if (link) {
    var url;
    if (link.substring(0,1)=='^') {
      url = link.substring(1);
      link = url;
    } else {
      url = node.relpath+link;
    }
    a.className = stripPath(link.replace('#',':'));
    if (link.indexOf('#')!=-1) {
      var aname = '#'+link.split('#')[1];
      var srcPage = stripPath(pathName());
      var targetPage = stripPath(link.split('#')[0]);
      a.href = srcPage!=targetPage ? url : "javascript:void(0)"; 
      a.onclick = function(){
        storeLink(link);
        if (!$(a).parent().parent().hasClass('selected'))
        {
          $('.item').removeClass('selected');
          $('.item').removeAttr('id');
          $(a).parent().parent().addClass('selected');
          $(a).parent().parent().attr('id','selected');
        }
        var anchor = $(aname);
        gotoAnchor(anchor,aname,true);
      };
    } else {
      a.href = url;
      a.onclick = function() { storeLink(link); }
    }
  } else {
    if (childrenData != null) 
    {
      a.className = "nolink";
      a.href = "javascript:void(0)";
      a.onclick = node.expandToggle.onclick;
    }
  }

  node.childrenUL = null;
  node.getChildrenUL = function() {
    if (!node.childrenUL) {
      node.childrenUL = document.createElement("ul");
      node.childrenUL.className = "children_ul";
      node.childrenUL.style.display = "none";
      node.li.appendChild(node.childrenUL);
    }
    return node.childrenUL;
  };

  return node;
}

function showRoot()
{
  var headerHeight = $("#top").height();
  var footerHeight = $("#nav-path").height();
  var windowHeight = $(window).height() - headerHeight - footerHeight;
  (function (){ // retry until we can scroll to the selected item
    try {
      var navtree=$('#nav-tree');
      navtree.scrollTo('#selected',0,{offset:-windowHeight/2});
    } catch (err) {
      setTimeout(arguments.callee, 0);
    }
  })();
}

function expandNode(o, node, imm, showRoot)
{
  if (node.childrenData && !node.expanded) {
    if (typeof(node.childrenData)==='string') {
      var varName    = node.childrenData;
      getScript(node.relpath+varName,function(){
        node.childrenData = getData(varName);
        expandNode(o, node, imm, showRoot);
      }, showRoot);
    } else {
      if (!node.childrenVisited) {
        getNode(o, node);
      } if (imm || ($.browser.msie && $.browser.version>8)) { 
        // somehow slideDown jumps to the start of tree for IE9 :-(
        $(node.getChildrenUL()).show();
      } else {
        $(node.getChildrenUL()).slideDown("fast");
      }
      if (node.isLast) {
        node.plus_img.src = node.relpath+"ftv2mlastnode.png";
      } else {
        node.plus_img.src = node.relpath+"ftv2mnode.png";
      }
      node.expanded = true;
    }
  }
}

function glowEffect(n,duration)
{
  n.addClass('glow').delay(duration).queue(function(next){
    $(this).removeClass('glow');next();
  });
}

function highlightAnchor()
{
  var aname = hashUrl();
  var anchor = $(aname);
  if (anchor.parent().attr('class')=='memItemLeft'){
    var rows = $('.memberdecls tr[class$="'+hashValue()+'"]');
    glowEffect(rows.children(),300); // member without details
  } else if (anchor.parent().attr('class')=='fieldname'){
    glowEffect(anchor.parent().parent(),1000); // enum value
  } else if (anchor.parent().attr('class')=='fieldtype'){
    glowEffect(anchor.parent().parent(),1000); // struct field
  } else if (anchor.parent().is(":header")) {
    glowEffect(anchor.parent(),1000); // section header
  } else {
    glowEffect(anchor.next(),1000); // normal member
  }
  gotoAnchor(anchor,aname,false);
}

function selectAndHighlight(hash,n)
{
  var a;
  if (hash) {
    var link=stripPath(pathName())+':'+hash.substring(1);
    a=$('.item a[class$="'+link+'"]');
  }
  if (a && a.length) {
    a.parent().parent().addClass('selected');
    a.parent().parent().attr('id','selected');
    highlightAnchor();
  } else if (n) {
    $(n.itemDiv).addClass('selected');
    $(n.itemDiv).attr('id','selected');
  }
  if ($('#nav-tree-contents .item:first').hasClass('selected')) {
    $('#nav-sync').css('top','30px');
  } else {
    $('#nav-sync').css('top','5px');
  }
  showRoot();
}

function showNode(o, node, index, hash)
{
  if (node && node.childrenData) {
    if (typeof(node.childrenData)==='string') {
      var varName    = node.childrenData;
      getScript(node.relpath+varName,function(){
        node.childrenData = getData(varName);
        showNode(o,node,index,hash);
      },true);
    } else {
      if (!node.childrenVisited) {
        getNode(o, node);
      }
      $(node.getChildrenUL()).css({'display':'block'});
      if (node.isLast) {
        node.plus_img.src = node.relpath+"ftv2mlastnode.png";
      } else {
        node.plus_img.src = node.relpath+"ftv2mnode.png";
      }
      node.expanded = true;
      var n = node.children[o.breadcrumbs[index]];
      if (index+1<o.breadcrumbs.length) {
        showNode(o,n,index+1,hash);
      } else {
        if (typeof(n.childrenData)==='string') {
          var varName = n.childrenData;
          getScript(n.relpath+varName,function(){
            n.childrenData = getData(varName);
            node.expanded=false;
            showNode(o,node,index,hash); // retry with child node expanded
          },true);
        } else {
          var rootBase = stripPath(o.toroot.replace(/\..+$/, ''));
          if (rootBase=="index" || rootBase=="pages" || rootBase=="search") {
            expandNode(o, n, true, true);
          }
          selectAndHighlight(hash,n);
        }
      }
    }
  } else {
    selectAndHighlight(hash);
  }
}

function removeToInsertLater(element) {
  var parentNode = element.parentNode;
  var nextSibling = element.nextSibling;
  parentNode.removeChild(element);
  return function() {
    if (nextSibling) {
      parentNode.insertBefore(element, nextSibling);
    } else {
      parentNode.appendChild(element);
    }
  };
}

function getNode(o, po)
{
  var insertFunction = removeToInsertLater(po.li);
  po.childrenVisited = true;
  var l = po.childrenData.length-1;
  for (var i in po.childrenData) {
    var nodeData = po.childrenData[i];
    po.children[i] = newNode(o, po, nodeData[0], nodeData[1], nodeData[2],
      i==l);
  }
  insertFunction();
}

function gotoNode(o,subIndex,root,hash,relpath)
{
  var nti = navTreeSubIndices[subIndex][root+hash];
  o.breadcrumbs = $.extend(true, [], nti ? nti : navTreeSubIndices[subIndex][root]);
  if (!o.breadcrumbs && root!=NAVTREE[0][1]) { // fallback: show index
    navTo(o,NAVTREE[0][1],"",relpath);
    $('.item').removeClass('selected');
    $('.item').removeAttr('id');
  }
  if (o.breadcrumbs) {
    o.breadcrumbs.unshift(0); // add 0 for root node
    showNode(o, o.node, 0, hash);
  }
}

function navTo(o,root,hash,relpath)
{
  var link = cachedLink();
  if (link) {
    var parts = link.split('#');
    root = parts[0];
    if (parts.length>1) hash = '#'+parts[1].replace(/[^\w\-]/g,'');
    else hash='';
  }
  if (hash.match(/^#l\d+$/)) {
    var anchor=$('a[name='+hash.substring(1)+']');
    glowEffect(anchor.parent(),1000); // line number
    hash=''; // strip line number anchors
  }
  var url=root+hash;
  var i=-1;
  while (NAVTREEINDEX[i+1]<=url) i++;
  if (i==-1) { i=0; root=NAVTREE[0][1]; } // fallback: show index
  if (navTreeSubIndices[i]) {
    gotoNode(o,i,root,hash,relpath)
  } else {
    getScript(relpath+'navtreeindex'+i,function(){
      navTreeSubIndices[i] = eval('NAVTREEINDEX'+i);
      if (navTreeSubIndices[i]) {
        gotoNode(o,i,root,hash,relpath);
      }
    },true);
  }
}

function showSyncOff(n,relpath)
{
    n.html('<img src="'+relpath+'sync_off.png" title="'+SYNCOFFMSG+'"/>');
}

function showSyncOn(n,relpath)
{
    n.html('<img src="'+relpath+'sync_on.png" title="'+SYNCONMSG+'"/>');
}

function toggleSyncButton(relpath)
{
  var navSync = $('#nav-sync');
  if (navSync.hasClass('sync')) {
    navSync.removeClass('sync');
    showSyncOff(navSync,relpath);
    storeLink(stripPath2(pathName())+hashUrl());
  } else {
    navSync.addClass('sync');
    showSyncOn(navSync,relpath);
    deleteLink();
  }
}

function initNavTree(toroot,relpath)
{
  var o = new Object();
  o.toroot = toroot;
  o.node = new Object();
  o.node.li = document.getElementById("nav-tree-contents");
  o.node.childrenData = NAVTREE;
  o.node.children = new Array();
  o.node.childrenUL = document.createElement("ul");
  o.node.getChildrenUL = function() { return o.node.childrenUL; };
  o.node.li.appendChild(o.node.childrenUL);
  o.node.depth = 0;
  o.node.relpath = relpath;
  o.node.expanded = false;
  o.node.isLast = true;
  o.node.plus_img = document.createElement("img");
  o.node.plus_img.src = relpath+"ftv2pnode.png";
  o.node.plus_img.width = 16;
  o.node.plus_img.height = 22;

  if (localStorageSupported()) {
    var navSync = $('#nav-sync');
    if (cachedLink()) {
      showSyncOff(navSync,relpath);
      navSync.removeClass('sync');
    } else {
      showSyncOn(navSync,relpath);
    }
    navSync.click(function(){ toggleSyncButton(relpath); });
  }

  $(window).load(function(){
    navTo(o,toroot,hashUrl(),relpath);
    showRoot();
  });

  $(window).bind('hashchange', function(){
     if (window.location.hash && window.location.hash.length>1){
       var a;
       if ($(location).attr('hash')){
         var clslink=stripPath(pathName())+':'+hashValue();
         a=$('.item a[class$="'+clslink.replace(/</g,'\\3c ')+'"]');
       }
       if (a==null || !$(a).parent().parent().hasClass('selected')){
         $('.item').removeClass('selected');
         $('.item').removeAttr('id');
       }
       var link=stripPath2(pathName());
       navTo(o,link,hashUrl(),relpath);
     } else if (!animationInProgress) {
       $('#doc-content').scrollTop(0);
       $('.item').removeClass('selected');
       $('.item').removeAttr('id');
       navTo(o,toroot,hashUrl(),relpath);
     }
  })
}

